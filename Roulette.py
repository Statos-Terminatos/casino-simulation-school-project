import random

# This is used to fixed the random generator so we can test the output
random.seed(3456)

"""
testing the pseudo random number generetor

for _ in range(0, 5):
    print(random.randrange(1, 10))
"""
